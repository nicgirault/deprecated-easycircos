form.easy-circos-form
  accordion(close-others='true')
    track-settings
    track-data
    track-position

    accordion-group.easy-circos-yellow
      accordion-heading Scale
      div.form-group
        label Min
          span.glyphicon.glyphicon-question-sign.easycircos-info(
            tooltip-placement="right"
            tooltip-append-to-body='true'
            tooltip-trigger='mouseenter : mouseleave'
            tooltip-html-unsafe='{{ help.min }}'
            aria-hidden='true'
          )
        div
          input.form-control(
            ng-model = 'currentTrack.conf.min'
            type = 'text'
            ng-change = 'render()'
          )

      div.form-group
        label Max
          span.glyphicon.glyphicon-question-sign.easycircos-info(
            tooltip-placement="right"
            tooltip-append-to-body='true'
            tooltip-trigger='mouseenter : mouseleave'
            tooltip-html-unsafe='{{ help.max }}'
            aria-hidden='true'
          )
        div
          input.form-control(
            ng-model = 'currentTrack.conf.max'
            type = 'text'
            ng-change = 'render()'
          )

      div.form-group
        input(
          ng-model = 'currentTrack.conf.logScale'
          type = 'checkbox'
          ng-change = 'render()'
        )
        label Log scale

    accordion-group.easy-circos-greenish
      accordion-heading Line
      div.form-group
        label Color
        input.form-control(
          ng-model = 'currentTrack.conf.color'
          type = 'color'
          ng-change = 'render()'
        )

      div.form-group
        label Fill color
        div.input-group
          span.input-group-addon
            input(
              type='checkbox'
              ng-model='currentTrack.conf.fill'
              ng-value='true'
              ng-change='render()'
            )
          input.form-control(
            ng-model = 'currentTrack.conf.fill_color'
            type = 'color'
            ng-change = 'render()'
          )

      div.form-group
        label Thickness
        input.form-control(
          ng-model = 'currentTrack.conf.thickness'
          type = 'number'
          step = '1'
          ng-change = 'render()'
        )

      div.form-group
        label Interpolation
          span.glyphicon.glyphicon-question-sign(
            tooltip-placement="right"
            tooltip-append-to-body='true'
            tooltip-trigger='mouseenter : mouseleave'
            tooltip-html-unsafe='{{ help.interpolation }}'
            aria-hidden='true'
          )
        select.form-control(
          ng-model = 'currentTrack.conf.interpolation'
          ng-options = 'type for type in interpolationTypes'
          ng-change = 'render()'
        )
        p {{ help[ currentTrack.conf.interpolation ] }}
    
    track-axes
    accordion-group.easy-circos-green
      accordion-heading Backgrounds
      backgrounds-form(
        backgrounds='currentTrack.conf.backgrounds'
        on-change-callback='render()'
      )

  a(href='https://docs.google.com/forms/d/1BQP-hnRP-eHL6_EUXWtitTF3CSZaZSpewsnalzot6wM/viewform?usp=send_form' target='_blank' style='margin: 10px;') Missing a parameter?
